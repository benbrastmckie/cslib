# Code Review Report

**Date**: 2026-06-30 (second pass — delta since `review-2026-06-30.md`)
**Scope**: all (health-signal delta review — re-verify morning findings, re-baseline markers)
**Reviewed by**: Claude

## Summary

- Files reviewed: whole-repo health census (`Cslib/**.lean`); targeted re-check of the morning
  review's one actionable finding (build barrel integrity) plus marker/axiom re-census
- Critical issues: 0
- High priority issues: 0 *(the morning review's single High issue is now resolved)*
- Medium priority issues: 1 (tracked proof debt — baseline only)
- Low priority issues: 1 (aspirational TODO notes — informational)

This is a formalization library, so "issues" are build-integrity, proof-debt (`sorry`), and
hygiene signals rather than runtime/security defects. A full review ran earlier today
(`review-2026-06-30.md`); this second pass re-verifies its findings after the `/todo` archival
commit and the automata work landed.

## Resolved Since Morning Review

### ✅ Broken fresh-clone / CI build (was High) — RESOLVED
**File**: `Cslib.lean:11`
**Morning finding**: the committed barrel imported `Cslib.Computability.Automata.DA.Concat`, but
`Concat.lean` was untracked, so a fresh clone / CI checkout of `main` could not build.
**Current state**: `git ls-files --error-unmatch Cslib/Computability/Automata/DA/Concat.lean`
now succeeds — the file is tracked. `Cslib/Foundations/Data/OmegaSequence/InfOcc.lean` is clean
(no uncommitted modifications). No untracked or modified `.lean` files remain under `Cslib/`.
The barrel and its imports are now consistent with the working tree. **No action required.**

## Medium Priority Issues

### Proof debt: ~119 `sorry` occurrences across 28 files (tracked baseline)
**File**: repo-wide (top contributors below)
**Description**: Census of word-boundary `sorry` in `Cslib/**.lean` (~119 occurrences, 28 files;
≈35 are live `by sorry` / standalone-tactic holes, the rest are docstring/comment references).
Top clusters:
- **Bimodal completeness pipeline**: `BXCanonical/Chronicle/ChronicleToCountermodel.lean` (17),
  `Bundle/SuccRelation.lean` (14), `ConservativeExtension/TemporalConservativity.lean` (7),
  `Bundle/UntilSinceCoherence.lean` (4), plus Frame/Completeness/Algebraic.
- **Propositional Tableau**: `Intuitionistic/Scheme.lean` (7), `Minimal/DecisionProcedure.lean` (8),
  `Metalogic/MinDecidability.lean` (8), `Metalogic/IntDecidability.lean` (7),
  Int/Min/Classical Completeness/Soundness/DecisionProcedure.
- **Temporal Tableau**: `Temporal/Tableau/Completeness.lean` (5).
**Impact**: Large but almost entirely *tracked* debt. Cluster → owning task mapping:
- Bimodal → tasks 36, 37 (blocked: discrete/continuous completeness port), 215, 275 (blocked).
- Propositional Tableau → task 317 (implementing).
- Temporal Tableau → tasks 425 (not_started), 426, 427 (partial).
- Previously-flagged *unowned* foundational sorries → task **431** (`audit_unowned_foundational_sorries`),
  which was created precisely to triage them.
**Recommended fix**: No new action — every cluster has an owning task. Recorded as the current
baseline. The count is essentially flat versus the morning census (no regression).

## Low Priority Issues

### 22 `TODO` notes in Lean sources (mostly aspirational, not defects)
**File**: repo-wide (e.g. `LinearLogic/CLL/Basic.lean`, `Crypto/.../OneTimePad.lean`,
`Foundations/Relation/Euclidean.lean`, `Computability/Machines/SingleTapeTuring/Basic.lean`)
**Description**: 22 `TODO` comments, 0 `FIXME`. The majority are forward-looking design notes
("upstream this to Mathlib as a FinEnum instance", "Takahashi translation would be a shorter
proof", "develop an attribute to dualize theorems") rather than broken code. A few annotate the
known bimodal-discrete sorries (`ChronicleToCountermodel.lean`, "task 36") and are already
captured by the proof-debt item above.
**Impact**: Informational. None block build or correctness.
**Recommended fix**: Leave as in-source notes. Convert to tasks via `/fix-it` only if/when a
specific note is scheduled for work.

## Code Quality Metrics

| Metric | Value | Status |
|--------|-------|--------|
| `sorry` occurrences | ~119 (28 files) | Info — tracked by tasks 36/37/215/275/317/425/426/427/431 |
| `sorry` live holes (`by sorry`) | ~35 | Info |
| TODO count | 22 | Info |
| FIXME count | 0 | OK |
| Real `axiom` declarations | 0 | OK (all 4 grep hits are docstring prose) |
| Barrel build integrity | Intact | OK (Concat.lean tracked; tree clean) |
| Untracked/modified `.lean` under `Cslib/` | 0 | OK |

## Roadmap Progress

`roadmap-integration.sh` ran cleanly: 0 phases parsed, 0 matches, 0 annotations. ROADMAP.md has
no `(Task N)` references or phase/status tables that cross-reference the current completed tasks,
so there is nothing to annotate this pass.

### Current Focus (from active task state)
| Status | Count | Notes |
|--------|-------|-------|
| not_started | 21 | Largest pool — propositional/modal hygiene + temporal tableau follow-ons |
| blocked | 8 | Bimodal completeness chain (36/37/215/275), inter-system (419), reconcile (400), etc. |
| researched | 5 | 226, 299, 404, 415, 430 — ready to plan/implement |
| implementing | 2 | 241 (mcnaughton_theorem), 317 (propositional_tableau_completeness) |
| partial | 2 | 426, 427 (temporal tableau) |
| pr_ready | 1 | 407 (mpl_base_structure_first_redesign) |
| expanded | 3 | 278, 279, 296 (decomposed) |

### Recommended Next Tasks
1. **407** `mpl_base_structure_first_redesign` — pr_ready; submit/finish via `/pr` (closest to done).
2. **317** `propositional_tableau_completeness` — implementing; advancing it retires the largest
   *unblocked* tableau sorry cluster (Min/Int/Classical decidability instances depend on it).
3. **426 / 427** `temporal_tableau_*` — partial; resume to convert partial progress into green phases.
4. **431** `audit_unowned_foundational_sorries` — triage the foundational sorries to confirm none
   are silently un-owned (keeps the proof-debt baseline honest).
5. **404 / 415 / 430** — researched; plan next to keep the modal-soundness / lifting / intexpan
   cleanup pipeline moving.

## Recommendations

1. **No remediation tasks needed from this pass.** The morning review's only actionable issue
   (broken barrel) is resolved, and all proof debt maps to existing tasks. Creating new tasks
   would duplicate 431 and the tracked cluster owners.
2. **Priority is finishing in-flight work, not opening new fronts**: clear the one `pr_ready`
   (407), then the two `implementing` (241, 317) and two `partial` (426, 427) tasks before
   pulling from the 21-task `not_started` backlog.
3. **Keep the barrel honest**: the morning failure class (barrel importing an untracked module)
   recurred once already (task 422's `ProofSystemMorphism.lean`); a pre-commit check that every
   `public import` in `Cslib.lean` resolves to a tracked file would prevent a third instance.
