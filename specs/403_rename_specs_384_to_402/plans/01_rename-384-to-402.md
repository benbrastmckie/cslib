# Implementation Plan: Task #403

- **Task**: 403 - Rename specs/384_modal_tableau directory to specs/402_modal_tableau
- **Status**: [COMPLETED]
- **Effort**: 0.25 hours
- **Dependencies**: None
- **Research Inputs**: specs/403_rename_specs_384_to_402/reports/01_rename-384-to-402.md
- **Artifacts**: plans/01_rename-384-to-402.md (this file)
- **Standards**: plan-format.md, status-markers.md, artifact-management.md, tasks.md
- **Type**: meta
- **Lean Intent**: false

## Overview

This is a mechanical bookkeeping rename: the task directory `specs/384_modal_tableau_soundness_gap_redesign/` carries a provisional number that no longer matches its canonical task number (402). The plan moves the directory with `git mv`, updates the two artifact-path strings in `specs/archive/state.json` that still reference the old prefix, and verifies that no actionable stale path references remain. No Lean source files and no current `specs/state.json` entries are touched.

### Research Integration

Research confirmed: (1) the source directory exists with 6 files and the target does not exist, so `git mv` is collision-free; (2) task 402 lives only in `specs/archive/state.json` (lines 1818, 1823), not in the current `specs/state.json`; (3) task 384 (`tableau_completeness_sorries`) is an independent task whose slug cannot collide with the `384_modal_tableau` string, so it is safe from any string replacement; (4) remaining `384_modal_tableau` occurrences in `specs/state.json` and `specs/TODO.md` are task 403's own description prose, not paths, and require no change.

### Prior Plan Reference

No prior plan.

### Roadmap Alignment

No ROADMAP.md consulted for this bookkeeping task (no roadmap context provided).

## Goals & Non-Goals

**Goals**:
- Rename `specs/384_modal_tableau_soundness_gap_redesign/` to `specs/402_modal_tableau_soundness_gap_redesign/` via `git mv` (preserving history).
- Update the two artifact-path strings in `specs/archive/state.json` (lines 1818, 1823) to the new prefix.
- Verify no actionable `384_modal_tableau` path references remain and that task 384 is untouched.

**Non-Goals**:
- Modifying current `specs/state.json` (task 402 is not present there; task 403's description prose is intentionally left as-is).
- Changing `specs/TODO.md` (description prose only).
- Touching task 384 (`tableau_completeness_sorries`) entries.
- Updating internal self-reference text inside the moved markdown artifacts (historical text, out of scope).
- Any Lean source changes.

## Risks & Mitigations

| Risk | Impact | Likelihood | Mitigation |
|------|--------|------------|------------|
| Editing the wrong state.json (current instead of archive) | M | L | Task 402 exists only in `archive/state.json`; confirm with `jq` before and after editing; current `state.json` is never opened for write. |
| String replacement accidentally affecting task 384 | M | L | Task 384's slug is `tableau_completeness_sorries`; the literal `384_modal_tableau` cannot match it. Replace by full path string, not bare `384`. |
| `git mv` collision with an existing target | L | L | Research confirmed target does not exist; verification step re-checks with `ls` before the move. |
| Leftover stale path references | L | L | Grep verification step after edits; remaining hits expected only in task 403 description prose. |

## Implementation Phases

**Dependency Analysis**:
| Wave | Phases | Blocked by |
|------|--------|------------|
| 1 | 1 | -- |

Single-phase plan; no parallelism.

### Phase 1: Rename Directory and Reconcile Archive State [COMPLETED]

**Goal**: Move the directory to the canonical 402 prefix, update the two archive state path strings, and verify the rename is complete and isolated.

**Tasks**:
- [x] Pre-check: confirm source exists and target does not — `ls -d specs/384_modal_tableau_soundness_gap_redesign specs/402_modal_tableau_soundness_gap_redesign 2>&1` (expect source present, target "No such file"). *(completed)*
- [x] Pre-check: confirm task 402 is in archive only — `jq '.active_projects[]? | select(.project_number==402) | .project_name' specs/archive/state.json` returns `modal_tableau_soundness_gap_redesign`; the same query on `specs/state.json` returns nothing. *(completed)*
- [x] Execute the move: `git mv specs/384_modal_tableau_soundness_gap_redesign specs/402_modal_tableau_soundness_gap_redesign`. *(completed)*
- [x] Update `specs/archive/state.json` lines 1818 and 1823: replace `384_modal_tableau_soundness_gap_redesign` with `402_modal_tableau_soundness_gap_redesign` in the two artifact-path values only (use exact-string Edit on each path; do not alter surrounding JSON). *(completed)*
- [x] Confirm archive JSON validity — `jq empty specs/archive/state.json` (no output = valid). *(completed)*
- [x] Verify task 384 untouched — `jq '.active_projects[]? | select(.project_number==384) | .project_name' specs/state.json` still returns `tableau_completeness_sorries`. *(completed)*

**Timing**: 0.25 hours

**Depends on**: none

**Files to modify**:
- `specs/384_modal_tableau_soundness_gap_redesign/` -> `specs/402_modal_tableau_soundness_gap_redesign/` - directory renamed via `git mv`.
- `specs/archive/state.json` - two artifact-path strings (lines 1818, 1823) updated to the 402 prefix.

**Files explicitly NOT modified**:
- `specs/state.json` (task 403 description prose, task 384 entry).
- `specs/TODO.md` (task 403 description prose).
- Internal text inside moved markdown artifacts (historical references).

**Verification**:
- `ls -d specs/402_modal_tableau_soundness_gap_redesign` succeeds; `ls -d specs/384_modal_tableau_soundness_gap_redesign` reports "No such file".
- `grep -rn "384_modal_tableau" specs/ --include="*.json" --include="*.md"` returns only task 403 description-prose hits (`specs/state.json` line ~979, `specs/TODO.md` lines ~46, ~358) and no path-string hits in `archive/state.json`.
- `jq empty specs/archive/state.json` exits cleanly.

## Testing & Validation

- [x] Source directory no longer exists; target directory exists with all 6 files (`find specs/402_modal_tableau_soundness_gap_redesign -type f | wc -l` == 6). *(completed)*
- [x] `grep -rn "384_modal_tableau" specs/ --include="*.json" --include="*.md"` shows no path strings in `specs/archive/state.json`. *(completed)*
- [x] `jq empty specs/archive/state.json` confirms valid JSON. *(completed)*
- [x] Task 384 entry in `specs/state.json` is unchanged (`tableau_completeness_sorries`). *(completed)*
- [x] `git status` shows the rename (renamed files under the 402 prefix) plus the `archive/state.json` edit, and nothing else unexpected. *(completed)*

## Artifacts & Outputs

- Renamed directory `specs/402_modal_tableau_soundness_gap_redesign/` (6 files, history preserved).
- Updated `specs/archive/state.json` with two corrected artifact paths.
- Commit: `task 403: rename 384_modal_tableau dir to 402_modal_tableau`.

## Rollback/Contingency

If verification fails or the wrong file was edited:
- Undo the directory move: `git mv specs/402_modal_tableau_soundness_gap_redesign specs/384_modal_tableau_soundness_gap_redesign`.
- Revert the archive edit: `git checkout specs/archive/state.json`.
- Since no commit is made until all verification passes, `git restore --staged` and `git checkout` on the affected paths fully reverts the working tree. No Lean build or downstream artifacts depend on this change, so rollback has no side effects.
