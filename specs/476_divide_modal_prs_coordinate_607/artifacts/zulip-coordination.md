**Thread**: CSLib > Modal Logic
**Reply to**: [@fmontesi](https://leanprover.zulipchat.com/#narrow/channel/513188-CSLib/topic/Modal.20Logic/near/607842603) (his 2 July message — "overwhelmed… take these one at a time… back 23 July")
**Status**: DRAFT — not posted. Requires EXPLICIT user approval before sending. Only send once the claims below are actually true at post time (task-475 accuracy discipline) — re-verify the current PR/CI state of [#607](https://github.com/leanprover/cslib/pull/607), [#648](https://github.com/leanprover/cslib/pull/648), [#649](https://github.com/leanprover/cslib/pull/649), [#662](https://github.com/leanprover/cslib/pull/662) and the layer split before sending, since any of these may have moved by approval time (fmontesi returns 23 July). This draft reports that **[#662](https://github.com/leanprover/cslib/pull/662) is now reworked to make both □ and ◇ primitive and re-stacked directly on [#607](https://github.com/leanprover/cslib/pull/607)**, and raises ONE open coordination question: the propositional `Proposition` type design ([#648](https://github.com/leanprover/cslib/pull/648)'s primitive-`bot` five-primitive type vs [#607](https://github.com/leanprover/cslib/pull/607)/Thomas Waring's four-primitive `atom ⊥` type). It keeps the necessitation/K signature point (12 June message) to a single reassurance clause and does not re-run the full argument.

**Re-verify before posting** (as of 2026-07-12):
- **[#607](https://github.com/leanprover/cslib/pull/607)** (`fmontesi/connectives`): CI-green; consolidates operators into `Foundations/Logic/Operators.lean` (`HasBox`/`HasDiamond`/`HasImp`/… + dynamic-logic typeclasses); modal `Proposition` is ◇-primitive `{atom, not, and, diamond}`; propositional `Proposition` is four-primitive `{atom, and, or, imp}` with `⊥ = atom ⊥` under `[Bot Atom]`. Note [#607](https://github.com/leanprover/cslib/pull/607) is currently behind main (based ~[#683](https://github.com/leanprover/cslib/pull/683); main is [#708](https://github.com/leanprover/cslib/pull/708)) — normal for a stack.
- **[#662](https://github.com/leanprover/cslib/pull/662)** (`feat/modal-formula-primitives`): DONE — reworked to add `box` as a primitive constructor alongside `diamond` (both primitive; `◇φ ↔ ¬□¬φ` now a derived lemma), reusing [#607](https://github.com/leanprover/cslib/pull/607)'s `Operators.lean`. Re-based to sit directly on [#607](https://github.com/leanprover/cslib/pull/607): PR base changed `main → fmontesi/connectives`, single clean commit, diff is 3 files in `Logics/Modal` (+39/−17), `mergeable=MERGEABLE`, CI running at draft time. Pre-stack tip backed up as `backup/662-pre-stack-jul12` (`8d7a061e`).
- **[#648](https://github.com/leanprover/cslib/pull/648)** (`feat/propositional-v2`) and **[#649](https://github.com/leanprover/cslib/pull/649)** (`feat/temporal-formula-propositional`): kept INDEPENDENT on latest upstream/main (`056cf937`, [#708](https://github.com/leanprover/cslib/pull/708)), CI-green. [#648](https://github.com/leanprover/cslib/pull/648) proposes a primitive-`bot` five-primitive `Proposition` that competes with [#607](https://github.com/leanprover/cslib/pull/607)'s four-primitive type (both edit `Logics/Propositional/Defs.lean`), so it is deliberately NOT stacked on [#607](https://github.com/leanprover/cslib/pull/607) pending the design question below. Before posting, confirm [#662](https://github.com/leanprover/cslib/pull/662)'s CI went green and the three PRs still report as described.

---

Hi @fmontesi,

No worries at all, and sorry about the volume. I'd be glad to join a CSLib meeting once you're back on the 23rd, though I'm in SF so 3:30am meeting time might be tricky for me, but happy to jump on a call.

To shrink the pile a little before then: I've reworked [#662](https://github.com/leanprover/cslib/pull/662) to sit directly on your [#607](https://github.com/leanprover/cslib/pull/607). I added □ as a primitive constructor alongside ◇ — so both modalities are primitive, with `◇φ ↔ ¬□¬φ` now a derived lemma — reusing your `Operators.lean` typeclasses. It's a small diff (three files in `Logics/Modal`, ~40 lines) and its PR base now points at `fmontesi/connectives`, so it reviews as just the modal delta on top of [#607](https://github.com/leanprover/cslib/pull/607), whenever you have a moment. Both primitive is what we'll want anyway for the intuitionistic and minimal systems (IK, CK); necessitation and K still touch only □, so the proof theory doesn't get any heavier.

There's one design question I'd value your and Thomas's view on before I stack the rest: **[#648](https://github.com/leanprover/cslib/pull/648) reworks `Logics/Propositional` to make `⊥` a primitive constructor** (a five-primitive `{atom, bot, imp, and, or}` with `¬A := A → ⊥`), whereas [#607](https://github.com/leanprover/cslib/pull/607)/Thomas's `Proposition` keeps four primitives with `⊥` as `atom ⊥` under `[Bot Atom]`. Since both touch the same `Proposition` type they can't simply layer — we'd need to pick one base. I've deliberately kept [#648](https://github.com/leanprover/cslib/pull/648) (and [#649](https://github.com/leanprover/cslib/pull/649), LTL) independent on main rather than have [#648](https://github.com/leanprover/cslib/pull/648) silently override your type; I'm happy either way and will rebase them onto [#607](https://github.com/leanprover/cslib/pull/607) once we settle which propositional basis to standardise on.

Everything else we can take one at a time, as you suggested — happy to walk through it whenever suits. Enjoy the time away, and talk on the 23rd.
