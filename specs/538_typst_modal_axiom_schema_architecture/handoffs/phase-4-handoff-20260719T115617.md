# Phase 4 Handoff: Sections 5-7 and the Source-Anchor Appendix

**Status**: COMPLETED

## What was done
- Section 5 (S5 = T+4+B): `s5Tags`/`kb5Tags` side-by-side table, the `decide`-false consequence
  explaining the missing KB5 -> S5 cube edge, and a "self-documenting property" remark.
- Section 6 (Design Rationale): a two-column Representation A vs. B comparison table replacing
  the source's prose trade-off bullets, plus a closing remark.
- Section 7 (Scope Boundary): the module docstring's Design Invariants excerpt via
  `#quote(block: true)`, a table of the four existing out-of-scope non-classical axiom families
  with their file anchors, and a "future instance, not a fork" remark.
- Appendix: a consolidated 22-row source-anchor table (File:lines | Declaration (section)),
  every line number grepped directly against live source during authoring.
- Confirmed `grep -nE "task [0-9]+|tasks [0-9]+|\(task [0-9]+\)"` against the full `.typ` file
  returns no matches.

## Bugs found and fixed during incremental compile/visual review
- Appendix table: same column-width-squeeze class of bug as Phases 2-3 (auto-width File column
  forced the Declaration column too narrow, plus an em-dash "--- Sec.\ N" pattern that
  line-broke awkwardly after "Sec."). Fixed with explicit fr-weighted columns, `8.7pt` text, and
  a compact "(§N, note)" citation format.
- Dropped an incidental "Phase 1 open item" phrase from one appendix cell -- not a literal
  task-number pattern, but out of caution around the no-task-references rule's spirit (ephemeral
  process metadata meaningless to a future reader).

## Next phase
Phase 5: final compile/verify, concision and fidelity review, AxiomB cross-check, and the
task-number grep (already run once here; Phase 5 re-confirms after any final touch-ups).
