# Code Review Report

**Date**: 2026-06-30
**Scope**: all (health-signal review — sorry/marker census, build integrity, roadmap, next-task recommendations)
**Reviewed by**: Claude

## Summary

- Files reviewed: whole-repo census (Lean sources under `Cslib/`); targeted inspection of build barrel and uncommitted WIP
- Critical issues: 0
- High priority issues: 1
- Medium priority issues: 3
- Low priority issues: 3

This is a formalization library, so "issues" are build-integrity, proof-debt (`sorry`), and
hygiene signals rather than runtime/security defects. A full per-file proof review of the
thousands of Lean files is out of scope for a single pass; this review censuses health markers
and cross-references them against the active task list.

## High Priority Issues

### Committed barrel imports an untracked file (broken fresh-clone / CI build)
**File**: `Cslib.lean:11`
**Description**: The committed barrel (`HEAD:Cslib.lean`) contains
`public import Cslib.Computability.Automata.DA.Concat`, but `Cslib/Computability/Automata/DA/Concat.lean`
is **untracked** (`git ls-files --error-unmatch` → "did not match any file"). A related file,
`Cslib/Foundations/Data/OmegaSequence/InfOcc.lean`, has uncommitted modifications.
**Impact**: A fresh clone or CI checkout of `main` cannot build — the barrel references a module
that does not exist in the repository. This is the same failure class that task 422 flagged for
the (now-resolved, committed) `ProofSystemMorphism.lean`.
**Recommended fix**: Either commit `Concat.lean` (+ `InfOcc.lean`) as part of the automata work
(tasks 428/429/430 touch this BuchiCongruence/automata area), or temporarily remove the
`DA.Concat` import line from `Cslib.lean` until the file lands. Do not leave HEAD in a state
where the barrel imports an untracked module.

## Medium Priority Issues

### Proof debt: 121 `sorry` occurrences across 27 files
**File**: repo-wide (top contributors below)
**Description**: Census of `sorry` in `Cslib/**.lean`:
- Bimodal completeness pipeline (~46): `BXCanonical/Chronicle/ChronicleToCountermodel.lean` (17),
  `Bundle/SuccRelation.lean` (14), `ConservativeExtension/TemporalConservativity.lean` (7),
  `Bundle/UntilSinceCoherence.lean` (4), plus Frame/RRelation/PointInsertion/Dense/InteriorOperators.
- Propositional Tableau (~46): `Intuitionistic/Scheme.lean` (11),
  `Minimal/DecisionProcedure.lean` (8), `Metalogic/MinDecidability.lean` (8),
  `Metalogic/IntDecidability.lean` (7), Int/Min/Classical Completeness/Soundness/DecisionProcedure.
- Temporal Tableau (4): `Temporal/Tableau/Completeness.lean`.
**Impact**: Large but largely *tracked* debt. The clusters map to known tasks: Bimodal →
36/37/215/275 (blocked on upstream completeness), Propositional Tableau → 317 (blocked),
Temporal Tableau → 425/426/427. The decidability-module sorries are documented (task 422:
tableau instances carry pre-existing 317 `sorryAx`; FMP defs are sorry-free).
**Recommended fix**: No new action — debt is owned by existing tasks. Tracked here as a baseline.

### A handful of `sorry`s with no obvious owning task
**File**: `Cslib/Foundations/Order/HilbertAlgebra.lean:1`,
`Cslib/Foundations/Logic/Metalogic/GenericMCS.lean:1`,
`Cslib/Languages/LambdaCalculus/Named/Untyped/Basic.lean:1`,
`Cslib/Logics/Propositional/Semantics/Algebra/CanAlgComplete.lean:1`,
`Cslib/Logics/LTL/Semantics/GNBA.lean:1`,
`Cslib/Logics/Propositional/NaturalDeduction/Normalization/Termination.lean` (2)
**Description**: These single/low-count sorries sit in Foundations and Languages files outside
the Bimodal/Tableau clusters, so they are not clearly covered by tasks 36/37/215/275/317/425-427.
**Impact**: Foundational sorries can silently propagate `sorryAx` into downstream `#print axioms`
checks. CanAlgComplete and GNBA in particular feed completeness/automata results.
**Recommended fix**: Audit each against the task list; spawn a small tracking task if any are
genuinely unowned (candidate for a new `cslib` task).

### Uncommitted source WIP left in the working tree
**File**: `Cslib/Foundations/Data/OmegaSequence/InfOcc.lean` (modified),
`Cslib/Computability/Automata/DA/Concat.lean` (untracked)
**Description**: Two automata/omega-sequence source files are dirty in the working tree (separate
from the specs/ archival just committed). The untracked one is the High issue above.
**Recommended fix**: Fold into the active automata task (428/429/430) commits, or stash, so HEAD
is self-consistent.

## Low Priority Issues

### 22 `TODO` markers in Lean sources
**Description**: 22 `TODO` comments remain in `Cslib/**.lean` (0 `FIXME`, 0 real `admit` — the one
`admit` match is the English word in a `PACLearning/Defs.lean` docstring). Informational.

### Agent tooling: `roadmap-integration.sh` fails
**File**: `.claude/scripts/roadmap-integration.sh:352`
**Description**: Invoking the script during this review aborted with
`/etc/profiles/per-user/benjamin/bin/python3: Argument list too long` at line 352 (likely passing
file content as an argv instead of via stdin/temp file). Roadmap auto-annotation was done manually.
**Recommended fix**: Patch the script to pass `state.json`/`ROADMAP.md` via stdin or a temp path
(candidate `meta` task).

### ROADMAP.md "Remaining" section is stale vs. recent decidability progress
**File**: `specs/ROADMAP.md:95-104`
**Description**: ROADMAP tracks the Bimodal-porting completeness frontier but does not reflect the
propositional decidability/FMP work that landed (tasks 370/411/421/422: Int/Min FMP decidability,
route reconciliation) or the structure-first propositional refactors (415-422). The "Completed"
table also predates the Embedding/typeclass consolidation already merged.
**Recommended fix**: Refresh ROADMAP "Completed"/"Remaining" tables (covered in spirit by tasks
389/390 documentation work).

## Code Quality Metrics

| Metric | Value | Status |
|--------|-------|--------|
| `sorry` occurrences | 121 (27 files) | Tracked (blocked tasks) |
| `TODO` count (Lean) | 22 | Info |
| `FIXME` count | 0 | OK |
| Real `admit` | 0 | OK |
| Build integrity (fresh clone) | FAIL | Barrel imports untracked `Concat.lean` |
| Active tasks | 45 | — |

## Roadmap Progress

### Completed Since Last Review (2026-06-29)
- 19 tasks archived today (`/todo`): propositional decidability/FMP (370, 411, 421, 422),
  structure-first embedding/conservativity (416, 417, 418, 420), sequent/fragment work (408, 410),
  IPL-base + PR prep (398, 399, 401), tableau/build unblock (385, 388), and reconciliation/meta
  (395, 403); abandoned 360, 384.

### Current Focus (dependency waves, from TODO.md)
| Wave | Representative actionable tasks | Notes |
|------|--------------------------------|-------|
| 1 | 299 [planned], 386 [researched], 404 [researched], 415 [researched], 407 [pr_ready], 428/429/430 [researched] | Unblocked; ready to advance |
| 2 | 317 [blocked], 300, 301 [blocked], 405, 409 | Mostly blocked on Wave 1 |
| 3 | 41, 241 [blocked], 275 [blocked], 375, 389 | Bimodal/foundations |
| 4-5 | 391-393, 412-414 | Hygiene / proof simplification |

### Recommended Next Tasks
1. **Fix build integrity** — commit or stash `Concat.lean`/`InfOcc.lean`, or drop the
   `DA.Concat` import from `Cslib.lean` (High; unblocks fresh-clone/CI builds).
2. **`/implement 299`** (modal_k_tableau) — already `[planned]`, Wave 1, ready to execute.
3. **`/implement 386`** (fix_lake_lint_errors_propositional) — `[researched]`, quick hygiene win.
4. **`/pr 407`** (mpl_base_structure_first_redesign) — `[pr_ready]`, awaiting user PR submission.
5. **`/plan 404` / `/plan 415`** — researched, ready to plan (soundness cleanup; lifting audit).

## Recommendations

1. Restore HEAD build integrity before any further implementation work (the untracked-import
   issue will break CI on the next push).
2. Keep the large `sorry` debt tracked under its owning blocked tasks (36/37/215/275/317/425-427);
   do not open duplicate cleanup tasks.
3. Audit the ~6 unowned foundational sorries (HilbertAlgebra, GenericMCS, CanAlgComplete, GNBA,
   LambdaCalculus/Basic, Termination) and spawn a tracking task only if genuinely orphaned.
4. Patch `roadmap-integration.sh` (argv overflow) so future reviews get automated roadmap sync.
