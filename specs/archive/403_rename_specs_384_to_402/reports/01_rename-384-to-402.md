# Research Report: Task #403 — Rename specs/384_modal_tableau to specs/402_modal_tableau

**Task**: 403 — rename_specs_384_to_402
**Started**: 2026-06-29T00:00:00Z
**Completed**: 2026-06-29T00:00:00Z
**Effort**: 30 minutes
**Dependencies**: None
**Sources/Inputs**: Codebase grep, state.json, archive/state.json, filesystem inspection
**Artifacts**: specs/403_rename_specs_384_to_402/reports/01_rename-384-to-402.md
**Standards**: report-format.md, subagent-return.md

## Executive Summary

- Source directory `specs/384_modal_tableau_soundness_gap_redesign/` exists and contains 6 files; no collision with target.
- Task 402's canonical state entry is in `specs/archive/state.json` (not current `state.json`) — its two artifact paths point to the old `384_modal_tableau_soundness_gap_redesign` prefix and must be updated there.
- Task 384 (tableau_completeness_sorries) exists independently in current `specs/state.json` and must NOT be touched.
- Exactly four external references to `384_modal_tableau` require updating: two artifact-path strings in `archive/state.json`; the other occurrences are task description text (not paths) in `state.json` and `TODO.md` and need not change.
- Three internal path strings inside the moved artifacts also contain the old directory name; updating them is optional cleanup but harmless if left as historical text.

## Context & Scope

Task 402 (`modal_tableau_soundness_gap_redesign`) was created to address a soundness-proof gap that blocked task 364. It was assigned number 402 during a merge to avoid colliding with the independently existing task 384 (`tableau_completeness_sorries`). However, the task directory on disk retained the original provisional name `384_modal_tableau_soundness_gap_redesign/`. This rename reconciles the directory name with the canonical task number. No Lean source files are involved.

## Findings

### 1. Source Directory — Exists, No Collision

**Path**: `specs/384_modal_tableau_soundness_gap_redesign/`
**Exists**: Yes

File inventory:
```
specs/384_modal_tableau_soundness_gap_redesign/
├── .orchestrator-handoff.json
├── .return-meta.json
├── handoffs/
│   └── 299-salvage-pointer.md
├── plans/
│   └── 01_per-branch-accessibility.md
├── reports/
│   └── 01_soundness-gap-redesign.md
└── summaries/
    └── 01_per-branch-accessibility-summary.md
```

**Target** `specs/402_modal_tableau_soundness_gap_redesign/` does NOT exist — git mv will proceed cleanly.

### 2. Task 384 — Independent Task, Must Not Be Touched

`specs/state.json` contains task 384 (`tableau_completeness_sorries`, status `abandoned`). Its `project_name` is `tableau_completeness_sorries` and its directory would be `specs/384_tableau_completeness_sorries/` (which does not conflict). **No changes required to task 384 entries.**

### 3. Task 402 — Location in Archive

Task 402 is **absent from current `specs/state.json`** (project numbers jump from 401 to 403 in the active list). It IS present in `specs/archive/state.json` at line 1805:

```
"project_number": 402,
"project_name": "modal_tableau_soundness_gap_redesign",
"status": "completed",
```

Its two artifact entries (lines 1818 and 1823) contain the old path:

| # | Line | Current path |
|---|------|-------------|
| 1 | 1818 | `specs/384_modal_tableau_soundness_gap_redesign/reports/01_soundness-gap-redesign.md` |
| 2 | 1823 | `specs/384_modal_tableau_soundness_gap_redesign/plans/01_per-branch-accessibility.md` |

Both must be updated to `specs/402_modal_tableau_soundness_gap_redesign/...`.

### 4. Complete Reference Inventory

All occurrences of `384_modal_tableau` outside the source directory itself:

| File | Line(s) | Type | Action Required |
|------|---------|------|----------------|
| `specs/archive/state.json` | 1818, 1823 | Artifact path strings in task 402 entry | **UPDATE** both paths |
| `specs/state.json` | 979 | Description text of task 403 (explanatory, not a path) | No change needed |
| `specs/TODO.md` | 46, 358 | Description text of task 403 | No change needed |

### 5. Internal Self-References Inside Moved Artifacts

Three markdown files inside the directory contain the old directory name as path references. After the git mv, these become stale text (not broken symlinks or imports). They are historical documentation — updating them is optional:

| File | Content |
|------|---------|
| `reports/01_soundness-gap-redesign.md` line 13 | `specs/384_modal_tableau_soundness_gap_redesign/reports/01_soundness-gap-redesign.md` (Artifacts field) |
| `plans/01_per-branch-accessibility.md` line 7 | `specs/384_modal_tableau_soundness_gap_redesign/reports/01_soundness-gap-redesign.md` (Research Inputs field) |
| `plans/01_per-branch-accessibility.md` line 322 | `specs/384_modal_tableau_soundness_gap_redesign/summaries/01_per-branch-accessibility-summary.md` |
| `summaries/01_per-branch-accessibility-summary.md` line 76 | Absolute path in a different worktree (`cslib-364/`), already stale |

## Decisions

- **Update archive/state.json, not current state.json**: Task 402 lives only in the archive; current state.json has no task 402 entry to update.
- **Leave description text alone**: Occurrences in task 403's description field (state.json and TODO.md) are explanatory prose, not artifact paths. They document the reason for the rename and need not be changed.
- **Internal artifact text**: Treat as optional cleanup; mark as out-of-scope for this bookkeeping task unless explicitly directed.

## Implementation Plan (for /plan phase)

**Step 1 — git mv**
```bash
git mv specs/384_modal_tableau_soundness_gap_redesign specs/402_modal_tableau_soundness_gap_redesign
```

**Step 2 — Update archive/state.json** (two path strings, lines 1818 and 1823):
Replace `384_modal_tableau_soundness_gap_redesign` with `402_modal_tableau_soundness_gap_redesign` at those two lines.

**Step 3 — Verify no remaining references**
```bash
grep -rn "384_modal_tableau" specs/ --include="*.json" --include="*.md"
```
Remaining occurrences should be only the description text in `state.json` (task 403) and `TODO.md` (task 403 description) — those are acceptable.

**Step 4 — Commit**
```bash
git add specs/402_modal_tableau_soundness_gap_redesign/ specs/archive/state.json
git commit -m "task 403: rename 384_modal_tableau dir to 402_modal_tableau"
```

## Risks & Mitigations

- **Risk**: Updating wrong state.json file. Mitigation: Task 402 is in `archive/state.json` only; confirm with `jq '.active_projects[] | select(.project_number == 402)'` on each file before editing.
- **Risk**: Breaking task 384 entries. Mitigation: Task 384's `project_name` is `tableau_completeness_sorries` — completely different slug; sed/string-replace on `384_modal_tableau` cannot affect it.
- **Risk**: Missing occurrences in `.lean` or other file types. Mitigation: Grep confirmed zero occurrences in `.lean` files; the only Lean directory is `Cslib/` which has no task directory references.

## Appendix

Search queries executed:
- `ls specs/384_modal_tableau_soundness_gap_redesign/`
- `ls specs/402_modal_tableau_soundness_gap_redesign/`
- `grep -rn "384_modal_tableau" specs/ --include="*.json" --include="*.md"`
- `jq '.active_projects[] | select(.project_number == 402)' specs/state.json`
- `jq '.active_projects[] | select(.project_number == 402)' specs/archive/state.json`
- `jq '.active_projects[] | select(.project_number == 384)' specs/state.json`
- `find specs/384_modal_tableau_soundness_gap_redesign/ -type f | sort`
