# Phase 3 Handoff: Sections 3-4 (unionSound Hinge, HasAxiom* Insulation Layer)

**Status**: COMPLETED

## What was done
- Section 3: five-row frame-correspondence table (Tag | Axiom schema | Frame condition | Lemma),
  with the modalB row using the Phase-1-confirmed `φ → □◇φ` expansion plus a footnote citing the
  exact live-source location and the classical-diamond-encoding caveat; `FrameValidatesTag`
  18-arm match and `unionSound` signature as literal Lean code blocks; a "the hinge, plainly"
  remark.
- Section 4: `HasAxiomT` representative signature, the base 4-level `extends` chain as a numbered
  list, a System/Hilbert-class name-mapping table for the modal-strength part (with a
  "same shape as the cube" remark referencing Figure 1, @fig:cube) instead of a second fletcher
  tree, and the `HasAxiomT Modal.HilbertS5` instance witness code block.

## Plan deviation (annotated per 4D-ii)
The plan offered "fletcher tree or nested list" for the extends hierarchy. Used a numbered list
for the unambiguous base chain, but a table + cube-reference remark (not a second fletcher tree)
for the modal-strength composites, because the source's own prose is imprecise about which single
ancestor each multi-differentiator class (TB, KB5, DB) extends -- fabricating specific tree edges
would not be a faithful transcription. This is more concise than a duplicate diagram and avoids
printing unverified structure.

## Bugs found and fixed during incremental compile/visual review
1. Frame-correspondence table: the `auto`-width Lemma column (containing long
   `Satisfies.modalFour_axiom`-style names) squeezed the Frame-condition column, causing overlap.
   Fixed by switching to all-fr columns, dropping the repeated `Satisfies.` prefix (stated once in
   the column header instead), and wrapping in `#text(size: 9pt)[...]`.
2. Two markdown-habit typos: `**bold**` (invalid double-star, Typst warning) corrected to `*bold*`
   (Typst's actual bold syntax); `@sec:tags--@sec:hinge` corrected to `Sections 1--3 (@sec:tags,
   @sec:cube, @sec:hinge)` after the `--` glued onto the label token.
- `typst compile` exit 0 after each fix; PDF visually re-inspected via `pdftoppm`.

## Next phase
Phase 4: Sections 5-7 (S5 = T+4+B, Design Rationale A vs. B, Scope Boundary) and the source-anchor
appendix.
