# Orchestration Notes — Task 442

## Architecture adjustment (discovered during Wave 2, P1a)
P1a added `import Cslib.Logics.Modal.Tableau.Completeness` into `FmpMeasure.lean` to reuse
`tryAllPropRules_pos/_neg` and `modal{And,Or,Imp,Neg}Of?_eq`. This is acyclic (Completeness does
NOT import FmpMeasure). CONSEQUENCE: the plan's P5 target file (`modalExpandBranches_hintikka` in
Completeness.lean using the measure) would force Completeness→FmpMeasure, creating a CYCLE.

RESOLUTION (settled, do not relitigate): P5a/P5b/P6 targets land in a NEW module
`Cslib/Logics/Modal/Tableau/CompletenessLoop.lean` importing FmpMeasure + Completeness + Soundness.
- modalExpandBranches_hintikka (P5b), modalStepBranch/combined-invariant preservation (P5a),
  modalTableau_complete + modalTableau_decides + Decidable (kValid) instance (P6) → CompletenessLoop.lean.
- Add CompletenessLoop.lean to Cslib.lean aggregator.
- DoD only requires the theorems to compile sorry/axiom-free; exact file is not load-bearing.

## Concurrency rule (post-P1a)
Because FmpMeasure now imports Completeness, do NOT build FmpMeasure-owning phases (P1b/P2/P3)
concurrently with any agent mid-editing Completeness.lean (P4). Serialize: wait for P4 commit first.

## Pre-existing repair (not in original plan)
Task-384 regression in SoundnessStep.lean `Proposition.beqToEq` fixed (commit 4153f885):
Proposition derives DecidableEq not BEq; replaced hand-rolled recursion with LawfulBEq.eq_of_beq.
Soundness + SoundnessStep now green. Needed for P6 whole-library green.
