# Phase 2 Handoff — Task 336 Parametric Modal Completeness Cascade

**Date**: 2026-06-24T18:00:00Z
**Phase completed**: Phase 2 — Parametric wrappers + K instantiation
**Session**: sess_1782319118_5be8e3_336

## What Was Done

### Metalogic/Completeness.lean (+3 theorems)

Added three parametric wrapper theorems after `strong_soundness` and `strong_completeness` (Phase 1):

1. **`strong_completeness_iff`** — biconditional combining both directions; takes same params as `strong_completeness` plus the `sound` callback; returns `ModalSemanticEntails FC Gamma phi ↔ ModalSetDerivable Axioms Gamma phi`.

2. **`compactness`** — given `h : ModalSemanticEntails FC Gamma phi`, produces `∃ L, (∀ x ∈ L, x ∈ Gamma) ∧ ModalSemanticEntails FC {ψ | ψ ∈ L} phi` by routing through `strong_completeness` + `strong_soundness`.

3. **`weak_completeness`** — takes validity `h_valid : ∀ World m, FC m → ∀ w, Satisfies m w phi` plus callbacks + `canonical_FC` and returns `Derivable Axioms phi`.

### Systems/K/Completeness.lean (cascade tail replaced)

Lines ~262-367 (the original 5-theorem cascade) replaced with:

- **Removed** `k_strong_soundness`, `k_strong_completeness`, `k_strong_completeness_iff`, `k_compactness`, `k_completeness` original bodies.
- **Added** `k_truth_lemma_applied` (private): pre-applies K's truth lemma callbacks for reuse.
- **Added** `k_sound_cb` (private): adapter wrapping `k_soundness` to match `strong_soundness` callback shape (`FC m` argument is `(fun _ => True) m = True`, discarded).
- **Replaced** all 5 public theorems with one-call delegations to the parametric cascade using named arguments `(Axioms := @KAxiom Atom) (FC := fun _ => True)` to fix universe inference issues with `trivial` for `canonical_FC`.

**Key fix**: Named-argument syntax `(FC := fun _ => True)` was required because Lean couldn't infer `FC` from `trivial : True` alone when `FC` is an implicit `∀ {World}, Model World Atom → Prop`.

**Untouched**: `k_derive_box_from_inconsistency`, `k_mcs_box_witness`, `k_truth_lemma` (lines 44-260).

## Verification

- `lake build Cslib.Logics.Modal.Metalogic.Completeness` — PASS
- `lake build Cslib.Logics.Modal.Metalogic.Systems.K.Completeness` — PASS (607 jobs)
- `lake exe lint-style` on both modified files — PASS (no output)
- `grep -n "^theorem k_"` confirms all 5 public names: `k_strong_soundness`, `k_strong_completeness`, `k_strong_completeness_iff`, `k_compactness`, `k_completeness`
- Zero sorries in modified files

## Next Phase

**Phase 3**: Refactor T-family (T, S4, S5, TB)

Files to modify:
- `Cslib/Logics/Modal/Metalogic/Systems/T/Completeness.lean`
- `Cslib/Logics/Modal/Metalogic/Systems/S4/Completeness.lean`
- `Cslib/Logics/Modal/Metalogic/Systems/S5/Completeness.lean`
- `Cslib/Logics/Modal/Metalogic/Systems/TB/Completeness.lean`

These systems use `truth_lemma` (from `Metalogic/Completeness.lean`, not K-specific). Each needs:
1. Define `<sys>FC` (refl; refl+trans; refl+trans+eucl; refl+symm) with docstring.
2. Prove `<sys>_canonical_FC` from `canonical_refl`/`canonical_trans`/`canonical_eucl`/`canonical_symm`.
3. Replace cascade tail with thin delegations.
4. **Preserve S5's public weak `*_completeness` inlined-∀ signature** (consumed by `Bimodal/.../ModalConservativity.lean:209`).

**Important**: Use named arguments `(Axioms := @<Sys>Axiom Atom) (FC := <sys>FC)` pattern discovered in Phase 2 to avoid FC inference issues.
