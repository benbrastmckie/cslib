Benjamin Brast-McKie: I've been implementing sound and complete Hilbert systems for propositional logic including the minimal, intuitionistic, and classical systems, establishing soundness and completeness for each. I also established the extensional equivalence with the intuitionistic and classical natural deduction systems. Still working on clean up, but here is what I have so far: https://github.com/benbrastmckie/cslib/tree/main/Cslib/Logics/PropositionalMatthew Doty: So I want to build on this PR, although all I need is assignment expression semantics at the moment. Would it be possible to make a smaller PR with just these semantics and maybe strong soundness?Thomas Waring: Hi Matthew, sorry I never got back to you in the other thread it completely slipped my mind! I think the right generality to develop assignment expressions + soundness (which i am interpreting as a Bool or Prop valued interpretation of a PL proposition) is in a docs#GeneralizedHeytingAlgebra — the proof is practically the same & this way it captures minimal and intuitionistic logic too. There are a couple of open PRs on ND already so I've held off PRing my work on the topic, but if you wanted to pick this up as a topic to work on i'd be very happy to see it moving!Thomas Waring: & hi also Benjamin :) I think your Hilbert-style approach is certainly interesting, as it happens i had been sketching something very similar in a branch which i find promising. i had an equivalent of your "derivation system" under the name "contextual inference system" with a similar interest in systems validating eg the deduction theorem, and also (in the same file) a simple hilbert-style derivation system.
i do think however that the design of something this general would require a fair amount of discussion, which makes PRing small pieces at a time important. that being said, i'd be excited to review steps in this direction!Thomas Waring: also Matthew, picking up on the points in the other thread — i think the idea of machinery to automate appeals to propositional logic is very interesting. the proof i had is constructive, and modulo the decidability of something being a classical tautology could construct an ND proof of propositions. i think the right way to do this for more general logics would be a class, though, like you said. i'd be very interested to see how this would work :) i unfortunately don't know anything about probability logic, though the folks at formalised formal logic might have more to say.Benjamin Brast-McKie: Hi Thomas and Matthey! I'm excited to contribute to contribute to this. I've been working backwards from the Bimodal logic that I established completeness for (which was all classical), so have been refactoring the propositional foundations to align with the ND system in CSLib. 
I established the deduction theorem and strong soundness and completeness for the minimal/inuitionistic/classical Hilbert systems and then proved their equivalence to the ND systems so that the ND systems inherit these results. This is typical since Hilbert systems are a lot easier to work with for establishing metalogical results. I've been trying to clean everything up and carve it up into small pieces for PRs. I would be curious to know what you think. Here are some links:
- deductionTheorem
- min_strong_completeness
- int_strong_completeness
- prop_strong_completenessBenjamin Brast-McKie: One issue regarding the syntax is that bot is not currently taken to be a primitive constructor in Proposition/, but rather simulated via [Bot Atom]. I created a PR to fix this, providing a foundation that will support the Hilbert systems for the Propositional/, Temporal/, and Bimodal/ logics and would be curious to get any thoughts.Matthew Doty: Thomas Waring said:

Hi Matthew, sorry I never got back to you in the other thread it completely slipped my mind! I think the right generality to develop assignment expressions + soundness (which i am interpreting as a Bool or Prop valued interpretation of a PL proposition) is in a docs#GeneralizedHeytingAlgebra — the proof is practically the same & this way it captures minimal and intuitionistic logic too. There are a couple of open PRs on ND already so I've held off PRing my work on the topic, but if you wanted to pick this up as a topic to work on i'd be very happy to see it moving!

Well, I had been thinking of proving Strong Soundness/Completeness, but @Benjamin Brast-McKie  beat me to it, so I thought I'd pivot to porting John Harrison's Handbook of Practical Logic and Automated Reasoning.
So far I've just given interpretation semantics for Atom -> Bool models for propositional formulae and CNF expressions, and proved that the Tseitin transformation from Proposition Atom to CNF is equisatisfiable.
The short-term goal would be to prove DPLL works as a decision procedure for propositional logic, if that makes sense. But we could also try to port and verify MiniSAT if there's interest.
I see that @Benjamin Brast-McKie 's models are from Atom -> Prop; would it be possible to switch to Atom -> Bool? In my case, I'd argue Atom -> Bool will be more portable for implementing DPLL for finding models.Matthew Doty: Thomas Waring said:

also Matthew, picking up on the points in the other thread — i think the idea of machinery to automate appeals to propositional logic is very interesting. the proof i had is constructive, and modulo the decidability of something being a classical tautology could construct an ND proof of propositions. i think the right way to do this for more general logics would be a class, though, like you said. i'd be very interested to see how this would work :) i unfortunately don't know anything about probability logic, though the folks at formalised formal logic might have more to say.

If you look at the Wikipedia page for probability logic, it's a bit of a mess, but a landmark result is Fagin et al.'s A Logic for Reasoning about Probabilities, where they give a complete axiomatization of Boolean combinations of inequalities in probability theory, and show that their theory is decidable and NP-Complete. If this seems appropriate, it might be nice to tackle formalizing this result in Lean...
Another idea is to define a finitely additive probability function Pr : Proposition Atom -> [0,1] and take some classical logic ⊢ and give the axioms Pr(ϕ) ≥ 0, ⊢ ϕ ⟹ Pr(ϕ) = 1, and Pr(ϕ) + Pr(ψ) = Pr(ϕ ∧ ψ) + Pr(ϕ ∨ ψ) (or something equivalent for the last axiom). A rather old result by Patrick Suppes I'm fond of in this setting is Γ ⊢ ϕ ⇔ ∀ Pr. ∑{1 − Pr(γ) | γ ∈ Γ} ≥ 1 − Pr(ϕ) for finite Γ. You can prove this using the MCS machinery that @Benjamin Brast-McKie is developing.Benjamin Brast-McKie: Matthew Doty said:

I see that @Benjamin Brast-McKie 's models are from Atom -> Prop; would it be possible to switch to Atom -> Bool? In my case, I'd argue Atom -> Bool will be more portable for implementing DPLL for finding models.

Hi Matthew, that's a great question. The core Valuation type needs to stay Atom → Prop because the canonical model construction in strong completeness uses fun p => atom p ∈ S where S is an MCS built via Lindenbaum/Zorn — that set membership is inherently Prop-valued with no DecidablePred. The same → Prop convention runs through the modal, temporal, and bimodal Kripke semantics.
To get Bool for DPLL, I think the answer is to have both: I've added a BoolEvaluate : (Atom → Bool) → Proposition Atom → Bool alongside the existing Evaluate, with a bridge lemma BoolEvaluate v φ = true ↔ Evaluate (fun a => v a = true) φ. That's a small addition that gives you computable evaluation connected to the existing metatheory including prop_strong_soundness.
Both layers are provided in the commit I just pushed to PR 648: Semantics/Basic.lean defines the Prop-valued Evaluate and Tautology, and Semantics/Bool.lean adds BoolEvaluate with the bridge lemma and a Decidable instance. Happy to adjust if you think there is a better way to structure it for DPLL module, or feel free to stack a PR. Let me know!Matthew Doty: Benjamin Brast-McKie said:

Hi Matthew, that's a great question. The core Valuation type needs to stay Atom → Prop because the canonical model construction in strong completeness uses fun p => atom p ∈ S where S is an MCS built via Lindenbaum/Zorn — that set membership is inherently Prop-valued with no DecidablePred. The same → Prop convention runs through the modal, temporal, and bimodal Kripke semantics.

I don't think there's a need to have two separate semantics.
You're already using attribute [local instance] Classical.propDecidable in StrongCompleteness.lean here.
Maybe just change canonicalValuation to
noncomputable def canonicalValuation (S : Set (PL.Proposition Atom)) : Valuation Atom :=
    fun p => decide (Proposition.atom p ∈ S)

...this does make the truth lemma more clumsy though; it would look like:
theorem prop_truth_lemma
    {S : Set (PL.Proposition Atom)}
    (h_mcs : PropSetMaximalConsistent PropositionalAxiom S) :
    (φ : PL.Proposition Atom) →
    (Evaluate (canonicalValuation S) φ = true ↔ φ ∈ S) := ...

On the other hand, I argue that this is still simpler and results in less code.Benjamin Brast-McKie: @ctchou also suggested that Bool.lean alone would suffice. The reason for Evaluate Prop-valued rather than collapsing everything through decide is uniformity with Kripke semantics since modal and temporal satisfaction (Satisfies) is inherently Prop-valued (accessibility relations, set membership at worlds), where the propositional Evaluate is the degenerate case — no worlds, no accessibility, same shape. If PL uses Bool but modal uses Prop, the FromPropositional embedding that lifts PL formulas into modal contexts has to bridge between the two evaluation types, which adds complexity rather than removing it.
One option would be to consolidate Bool.lean and Basic.lean into one file, keeping both evaluators with BoolEvaluate_eq_iff bridging them. The Modal/ and Temporal/ logics build on Evaluate (Prop-valued), while BoolEvaluate stays available for computable procedures like DPLL/SAT. I'm curious to get your thoughts on this.Matthew Doty: It looks like @Thomas Waring has Prop based semantics in mind as well: https://github.com/leanprover/cslib/pull/587/changes#diff-78912bb72e1c7000f8203f6312380447681f23b039101a86156ed6e3015836ccR35-R38
It also appears ωAcceptor is Prop based: https://github.com/leanprover/cslib/blob/70c5bf5874a1a511b32ad1ae605c07a13345d071/Cslib/Computability/Automata/Acceptors/OmegaAcceptor.lean#L17
Having all semantics be Prop based is awkward for decision procedures, where Evaluate model cnf where cnf : List (List (Literal Atom)) would naturally return Bool...Thomas Waring: not exactly, that PR is still a draft, but the equivalent of the Bool semantics you suggest would be a HasInterp instance with Ground = Bool, the Prop part would just be saying that a proposition A is valid in that model if interp A = true — if we were to implement something like the classes i wrote in that PR i think we should absolutely have a way to define a computable evaluation function.
as i mentioned in a comment on cslib#648 and above i think the way to resolve the Prop / Bool issue is to define the interpretation for any docs#GeneralizedHeytingAlgebra, the typeclass inference would be able to specialise to either of those cases, and the soundness proof would go through for the general case. that way we also get Heyting algebra semantics for non-classical logics too.Matthew Doty: Thomas Waring said:

that way we also get Heyting algebra semantics for non-classical logics too.

I think the zoo of non-classical logics may not always abide by Heyting algebra semantics; linear logics, for instance, don't always obey a ≤ b ⇨ c ↔ a ⊓ b ≤ c and instead have a separate residual for ⇨.
But now that I reread HasInterp, this looks like a good abstraction.Thomas Waring: right, sure — i was referring to semantics specifically for PL in this case. there a generalized heyting algebra is exactly the structure you need to prove the soundness theorem.
Thomas Waring: right, sure — i was referring to semantics specifically for PL in this case. there a generalized heyting algebra is exactly the structure you need to prove the soundness theorem.Matthew Doty: Might be more appropriate to call it Cslib.Logic.Structural instead of Cslib.Logic.PL, but that's just being pedantic.
I do agree with @Ching-Tsun Chou about a separate bot constructor.
But with that in mind, I agree with @Thomas Waring that it would be nice if we could just have the following polymorphic Evaluate (although for HeytingAlgebras rather than GeneralizedHeytingAlgebras):
def Evaluate {Atom A : Type u} [HeytingAlgebra A]
    (v : Atom → A) : Proposition Atom → A
  | .atom p => v p
  | .bot => ⊥
  | .imp φ ψ => Evaluate v φ ⇨ Evaluate v ψ
  | .and φ ψ => Evaluate v φ ⊓ Evaluate v ψ
  | .or φ ψ => Evaluate v φ ⊔ Evaluate v ψ
Thomas Waring: with that definition of evaluate completeness is no longer true for minimal logic — this (i think) is why Benjamin's Kripke definitions need separate fields for the valuation of atoms and of bottom, which is avoided by just making ⊥ an atom itself.
for reference, this file has my development of algebraic semantics, including the evaluation function i propose — the general completeness theorem is:
theorem Theory.complete [Inhabited Atom] {A : Proposition Atom} :
    DerivableIn T A ↔
    ∀ {H : Type u} [GeneralizedHeytingAlgebra H] {v : Valuation Atom H}, (v ⊨ T) → v ⊨ A

where v ⊨ T (resp v ⊨ A) denotes that every axiom (resp the proposition A) is mapped to ⊤ under the valuation v. i find this very neat, but there is a tradeoff of course, you can remove the v ⊨ T hypothesis for T = IPL (resp CPL) by requiring that H is a HeytingAlgebra (resp BooleanAlgebra) and that v ⊥ = ⊥. (Note that in Benjamin's development a similar thing happens, where ISemanticConsequence specialises IForces by requiring that ⊥ is never forced.)
